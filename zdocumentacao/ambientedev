# Guia de Rotina de Desenvolvimento

Este guia é para membros da equipe que **já passaram pela configuração inicial** e estão voltando para editar o código.

## 1. 🏁 Pré-requisito: Verifique o PostgreSQL

Antes de abrir o código, garanta que o seu servidor de banco de dados PostgreSQL está em execução.

1.  Abra o painel "Serviços" do Windows (`Win + R`, digite `services.msc`).
2.  Procure por `postgresql-x64-XX` (ex: `postgresql-x64-17`).
3.  Se o "Status" não for "Em Execução", clique com o botão direito e selecione **"Iniciar"**.

* (Na maioria das instalações, este serviço inicia automaticamente com o Windows).

---

## 2. 🚀 Iniciando o Backend (Django)

Para ligar o servidor da API.

1.  **Abra o Terminal** e navegue até a pasta do backend:
    ```bash
    cd caminho/para/Aplicativo-Oncologico/backend
    ```

2.  **Ative o Ambiente Python:**
    ```bash
    mamba activate apponco
    ```

3.  **(Recomendado) Atualize o Código:**
    Garanta que você tem a versão mais recente do código-fonte.
    ```bash
    git pull origin dev
    ```
    *(Ou qualquer que seja sua branch principal de desenvolvimento).*

4.  **(Se houver mudanças) Instale/Atualize Pacotes:**
    Se o `git pull` atualizou o arquivo `requirements.txt`:
    ```bash
    pip install -r requirements.txt
    ```

5.  **(Se houver mudanças) Atualize o Banco de Dados:**
    Se o `git pull` atualizou os `models.py` (ou seja, houver novas "migrations"):
    ```bash
    python manage.py migrate
    ```

6.  **Inicie o Servidor:**
    ```bash
    python manage.py runserver
    ```
    *O backend estará rodando em `http://127.0.0.1:8000/`*

---

## 3. 📱 Iniciando o Frontend (React Native)

Para visualizar o aplicativo no seu celular.

1.  **Abra um *Novo Terminal*** (deixe o backend rodando).
2.  Navegue até a pasta do frontend:
    ```bash
    cd caminho/para/Aplicativo-Oncologico/app-onco-frontend
    ```

3.  **(Recomendado) Atualize o Código:**
    ```bash
    git pull origin dev
    ```

4.  **(Se houver mudanças) Instale/Atualize Pacotes:**
    Se o `git pull` atualizou o arquivo `package.json`:
    ```bash
    npm install
    ```

5.  **Inicie o Expo:**
    ```bash
    npx expo start
    ```
    *Isso abrirá uma aba no navegador com um QR Code.*

6.  **Visualize o App:**
    Abra o aplicativo **Expo Go** no seu celular e escaneie o QR Code.